<div class="flex items-center justify-center min-h-screen bg-gradient-to-b from-slate-900 via-rose-950/70 to-slate-950">
  <div class="w-full px-4 sm:px-6">
    <div class="mx-auto w-full max-w-xl rounded-2xl border-0 sm:border bg-transparent sm:bg-pink-50 py-12 px-6 sm:px-10">
      <div class="space-y-8">
        <h2 class="text-4xl font-bold text-center">パスワードをお忘れですか?</h2>

        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "space-y-6" }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="field flex flex-col space-y-2">
            <%= f.label :email %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "bg-white border rounded-xl py-4 px-6" %>
          </div>

          <div class="actions">
            <%= f.submit "送信", class: "w-full text-white rounded-xl px-6 py-4 bg-blue-500 hover:bg-blue-600 transition" %>
          </div>
        <% end %>

        <div class="text-center">
          <%#= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>
