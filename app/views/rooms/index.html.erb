<%= render "shared/flash_message" %>

<div class="min-h-screen bg-gradient-to-b from-slate-900 via-rose-950/70 to-slate-950">
  <div class="w-full max-w-5xl mx-auto px-4 py-12 space-y-10">
    <div class="text-center text-white space-y-3">
      <h1 class="text-3xl sm:text-4xl font-bold">進行中の部屋</h1>
      <p class="text-base text-rose-100/80">
        フォロワーと一緒に作っていることわざの進捗をここから確認できます。
      </p>
    </div>

    <% if @rooms.any? %>
      <div class="space-y-6">
        <% @rooms.each do |room| %>
          <% proverb = room.proverb %>
          <% status = proverb.status  %>
          <% status_label = { "draft" => "準備中", "in_progress" => "作成中" }[status] %>
          <% status_color = {
               "draft" => "bg-gray-200 text-gray-800",
               "in_progress" => "bg-yellow-200 text-yellow-900",
             }[status] %>

          <div class="bg-white/95 border border-pink-100 rounded-2xl px-6 py-6 shadow-lg shadow-rose-950/30">
            <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
              <div>
                <p class="text-xs font-semibold text-gray-500">作成日</p>
                <p class="text-lg font-semibold text-gray-900"><%= room.created_at.strftime("%Y/%m/%d") %></p>
              </div>
              <span class="inline-flex items-center justify-center px-4 py-1 text-sm font-semibold rounded-full <%= status_color %>">
                <%= status_label %>
              </span>
            </div>

            <div class="mt-6 grid gap-4 sm:grid-cols-2">
              <% room.room_users.each do |room_user| %>
                <% role_label = room_user.role == "word_giver" ? "言葉担当" : "ことわざ担当" %>
                <% participant = room_user.user %>
                <% avatar_text = participant&.name.to_s.first.presence || "?" %>
                <%= link_to user_path(participant),
                            class: "flex items-start gap-3 bg-rose-50/60 border border-rose-100 rounded-2xl px-4 py-3 hover:bg-rose-100/70 transition" do %>
                  <div class="flex items-center justify-center w-12 h-12 rounded-full bg-pink-200 text-pink-800 font-bold text-lg overflow-hidden border border-pink-200">
                    <% if participant&.avatar&.attached? %>
                      <%= image_tag participant.avatar.variant(resize_to_fill: [48, 48]),
                                    class: "w-12 h-12 object-cover" %>
                    <% else %>
                      <span><%= avatar_text %></span>
                    <% end %>
                  </div>
                  <div class="flex flex-col">
                    <span class="text-xs font-semibold tracking-wide text-rose-400/80"><%= role_label %></span>
                    <span class="text-lg font-semibold text-gray-900"><%= participant&.name %></span>
                  </div>
                <% end %>
              <% end %>
            </div>

            <div class="mt-6 bg-pink-50/70 border border-pink-100 rounded-2xl px-5 py-4">
              <p class="text-xs font-semibold text-rose-400/80">現在のことば</p>
              <div class="mt-2 flex flex-wrap gap-6 text-gray-900 font-medium">
                <span>言葉1：<%= proverb.word1.presence || "未入力" %></span>
                <span>言葉2：<%= proverb.word2.presence || "未入力" %></span>
                <span>タイトル：<%= proverb.title.presence || "未入力" %></span>
              </div>
            </div>

<div class="mt-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">

  <%= link_to edit_room_proverb_path(room, proverb),
              class: "inline-flex items-center justify-center gap-2 rounded-2xl bg-blue-500 px-6 py-2.5 text-sm font-semibold text-white transition hover:bg-blue-600" do %>
    <i class="fa-solid fa-pen-to-square"></i>
    <span>部屋に入る</span>
  <% end %>
</div>
            </div>
        <% end %>
      </div>
    <% else %>
      <div class="bg-white/90 border border-pink-100 rounded-3xl px-8 py-12 text-center shadow-lg shadow-rose-950/30 space-y-6">
        <p class="text-2xl font-semibold text-gray-900">まだ進行中の部屋はありません</p>
        <p class="text-gray-600">
          フォローしているユーザーを招待して、一緒にことわざを作りましょう。
        </p>
        <div class="flex flex-col gap-3 sm:flex-row sm:justify-center">
          <%= link_to "部屋を作成する",
                      new_room_path,
                      class: "inline-flex items-center justify-center gap-2 rounded-2xl bg-pink-500 px-6 py-3 text-sm font-semibold text-white transition hover:bg-pink-600" %>
          <%= link_to "ユーザーを探す",
                      users_path,
                      class: "inline-flex items-center justify-center gap-2 rounded-2xl border border-pink-300 px-6 py-3 text-sm font-semibold text-pink-600 transition hover:bg-pink-50" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
